<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="v"
            type="android.view.View"/>

        <variable
            name="presenter"
            type="com.transfer.pay.ui.fragments.transactionhistory.TransactionHistoryPresenter"/>

        <variable
            name="transaction"
            type="com.transfer.pay.models.Transaction"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <ImageView
            android:id="@+id/deleteAllIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentRight="true"
            android:background="@drawable/ic_garbage"
            android:onClick="@{()->presenter.onAllTransactionDeleteClick()}"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/transactionHistorylist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/deleteAllIcon"/>

    </RelativeLayout>
</layout>